import { RateDirective } from './rate.directive';

describe('RateDirective', () => {
  it('should create an instance', () => {
    const directive = new RateDirective();
    expect(directive).toBeTruthy();
  });
});
